{
  "name": "arkivia-collab-todo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:backend": "npm run dev -w @arkivia/app-backend",
    "dev:frontend": "npm run dev -w @arkivia/app-frontend",
    "build": "npm --workspaces run build",
    "typecheck": "npm --workspaces run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
